"""
Updated Accounting Module with Cooperative Chart of Accounts
Integrates standard cooperative accounting practices
\"\"\"\n\nimport sqlite3\nfrom datetime import datetime\n\nclass AccountingManager:\n    def __init__(self, db_handler):\n        self.db = db_handler\n    \n    # COOPERATIVE CHART OF ACCOUNTS INTEGRATION\n    # Key Account Codes for Common Transactions\n    \n    COOPERATIVE_ACCOUNTS = {\n        # Assets\n        'CASH': '1010',\n        'BANK': '1020',\n        'MEMBER_RECEIVABLES': '1100',\n        'INVENTORY': '1300',\n        'EQUIPMENT': '1840',\n        'ACCUMULATED_DEPRECIATION': '1900',\n        \n        # Liabilities\n        'ACCOUNTS_PAYABLE': '3010',\n        'MEMBER_DEPOSITS': '3300',\n        'MEMBER_SAVINGS': '3320',\n        'MEMBER_TIME_DEPOSITS': '3330',\n        \n        # Equity - Cooperative Specific\n        'SHARE_CAPITAL': '4010',\n        'ENTRANCE_FEES': '4110',\n        'LEGAL_RESERVE': '4210',\n        'EDUCATION_RESERVE': '4220',\n        'PATRONAGE_REFUND_PAYABLE': '3340',\n        \n        # Revenue - Member vs Non-Member\n        'MEMBER_SALES': '5010',\n        'NON_MEMBER_SALES': '5110',\n        'MEMBER_SERVICES': '5020',\n        'INTEREST_INCOME': '5210',\n        \n        # Expenses\n        'SALARIES': '7010',\n        'RENT': '7110',\n        'DEPRECIATION': '7310',\n        'MEMBER_TRAINING': '7500',\n        'INTEREST_EXPENSE': '8010',\n    }\n    \n    def create_account(self, name, account_type, account_code=None):\n        \"\"\"Create account with optional cooperative code\"\"\"\n        try:\n            query = \"\"\"\n                INSERT INTO accounts (name, account_type, account_code, balance, created_date)\n                VALUES (?, ?, ?, 0, ?)\n            \"\"\"\n            self.db.cursor.execute(query, (name, account_type, account_code, datetime.now().isoformat()))\n            self.db.conn.commit()\n            return self.db.cursor.lastrowid\n        except Exception as e:\n            print(f\"Error creating account: {e}\")\n            return None\n    \n    def record_transaction(self, from_account, to_account, amount, description, transaction_type=\"Transfer\"):\n        \"\"\"Record a transaction between two accounts\"\"\"\n        try:\n            # Get account IDs\n            from_id = self._get_account_id(from_account)\n            to_id = self._get_account_id(to_account)\n            \n            if not from_id or not to_id:\n                print(\"Invalid account\")\n                return None\n            \n            # Insert transaction\n            query = \"\"\"\n                INSERT INTO transactions \n                (from_account_id, to_account_id, amount, transaction_type, description, transaction_date)\n                VALUES (?, ?, ?, ?, ?, ?)\n            \"\"\"\n            self.db.cursor.execute(query, (\n                from_id, to_id, amount, transaction_type, description, datetime.now().isoformat()\n            ))\n            \n            # Update balances\n            self.db.cursor.execute(\"UPDATE accounts SET balance = balance - ? WHERE id = ?\", (amount, from_id))\n            self.db.cursor.execute(\"UPDATE accounts SET balance = balance + ? WHERE id = ?\", (amount, to_id))\n            \n            self.db.conn.commit()\n            return self.db.cursor.lastrowid\n        except Exception as e:\n            print(f\"Error recording transaction: {e}\")\n            return None\n    \n    def record_member_deposit(self, member_id, amount, deposit_type=\"Savings\"):\n        \"\"\"Record a member deposit (cash deposit)\"\"\"\n        # Debit: Cash\n        # Credit: Member Deposits Payable\n        description = f\"Member {member_id} {deposit_type} deposit\"\n        \n        return self.record_transaction(\n            from_account='1010',  # Cash\n            to_account='3320' if deposit_type == \"Savings\" else '3330',  # Member Deposits\n            amount=amount,\n            description=description,\n            transaction_type=\"Member Deposit\"\n        )\n    \n    def record_member_withdrawal(self, member_id, amount):\n        \"\"\"Record a member withdrawal\"\"\"\n        description = f\"Member {member_id} withdrawal\"\n        \n        return self.record_transaction(\n            from_account='3320',  # Member Deposits\n            to_account='1010',    # Cash\n            amount=amount,\n            description=description,\n            transaction_type=\"Member Withdrawal\"\n        )\n    \n    def record_member_share_purchase(self, member_id, share_amount):\n        \"\"\"Record member purchasing shares (capital contribution)\"\"\"\n        description = f\"Member {member_id} share capital purchase\"\n        \n        return self.record_transaction(\n            from_account='1010',  # Cash (from member)\n            to_account='4010',    # Share Capital\n            amount=share_amount,\n            description=description,\n            transaction_type=\"Share Purchase\"\n        )\n    \n    def record_sales_to_member(self, member_id, amount, paid=True):\n        \"\"\"Record sales to member (on credit or cash)\"\"\"\n        if paid:\n            # Cash sale to member\n            return self.record_transaction(\n                from_account='1010',  # Cash\n                to_account='5010',    # Member Sales Revenue\n                amount=amount,\n                description=f\"Cash sale to member {member_id}\",\n                transaction_type=\"Member Sale\"\n            )\n        else:\n            # Credit sale to member\n            return self.record_transaction(\n                from_account='1100',  # Member Receivables\n                to_account='5010',    # Member Sales Revenue\n                amount=amount,\n                description=f\"Credit sale to member {member_id}\",\n                transaction_type=\"Member Sale\"\n            )\n    \n    def record_patronage_refund(self, member_id, refund_amount):\n        \"\"\"Record patronage refund to member\"\"\"\n        # Patronage refunds are based on member participation/volume\n        description = f\"Patronage refund to member {member_id}\"\n        \n        return self.record_transaction(\n            from_account='4500',  # Patronage Refund Reserve\n            to_account='3340',    # Patronage Refunds Payable\n            amount=refund_amount,\n            description=description,\n            transaction_type=\"Patronage Refund\"\n        )\n    \n    def allocate_surplus_to_reserves(self, surplus_amount):\n        \"\"\"Allocate cooperative surplus to statutory reserves\"\"\"\n        transactions = []\n        \n        # Typical allocation percentages (can be customized)\n        legal_reserve_pct = 0.10  # 10% to legal reserve\n        education_reserve_pct = 0.05  # 5% to education reserve\n        patronage_reserve_pct = 0.30  # 30% to patronage refund\n        \n        # 1. Legal Reserve\n        legal_amount = surplus_amount * legal_reserve_pct\n        transactions.append(self.record_transaction(\n            from_account='4420',  # Current Year Net Income\n            to_account='4210',    # Legal Reserve Fund\n            amount=legal_amount,\n            description=\"Transfer to Legal Reserve\",\n            transaction_type=\"Reserve Allocation\"\n        ))\n        \n        # 2. Education Reserve\n        education_amount = surplus_amount * education_reserve_pct\n        transactions.append(self.record_transaction(\n            from_account='4420',\n            to_account='4220',    # Education and Training Reserve\n            amount=education_amount,\n            description=\"Transfer to Education Reserve\",\n            transaction_type=\"Reserve Allocation\"\n        ))\n        \n        # 3. Patronage Refund Reserve\n        patronage_amount = surplus_amount * patronage_reserve_pct\n        transactions.append(self.record_transaction(\n            from_account='4420',\n            to_account='4500',    # Patronage Refund Reserve\n            amount=patronage_amount,\n            description=\"Transfer to Patronage Refund Reserve\",\n            transaction_type=\"Reserve Allocation\"\n        ))\n        \n        return transactions\n    \n    def record_depreciation(self, asset_code, depreciation_amount, asset_name):\n        \"\"\"Record monthly depreciation expense\"\"\"\n        return self.record_transaction(\n            from_account='7300',  # Depreciation Expense\n            to_account='1900',    # Accumulated Depreciation\n            amount=depreciation_amount,\n            description=f\"Monthly depreciation: {asset_name}\",\n            transaction_type=\"Depreciation\"\n        )\n    \n    def get_member_transaction_history(self, member_id, limit=50):\n        \"\"\"Get all transactions related to a member\"\"\"\n        query = \"\"\"\n            SELECT * FROM transactions\n            WHERE description LIKE ?\n            ORDER BY transaction_date DESC\n            LIMIT ?\n        \"\"\"\n        results = self.db.cursor.execute(query, (f\"%{member_id}%\", limit)).fetchall()\n        return results\n    \n    def get_account_balance(self, account_code):\n        \"\"\"Get balance of account by cooperative code\"\"\"\n        query = \"SELECT balance FROM accounts WHERE account_code = ?\"\n        result = self.db.cursor.execute(query, (account_code,)).fetchone()\n        return result[0] if result else 0\n    \n    def get_cooperative_financial_summary(self):\n        \"\"\"Generate financial summary for cooperative\"\"\"\n        summary = {\n            'total_assets': self._sum_account_range('1000', '1999'),\n            'total_liabilities': self._sum_account_range('3000', '3699'),\n            'member_equity': self.get_account_balance('4010'),\n            'legal_reserves': self.get_account_balance('4210'),\n            'education_reserves': self.get_account_balance('4220'),\n            'patronage_payable': self.get_account_balance('3340'),\n            'member_deposits': self.get_account_balance('3320'),\n            'member_receivables': self.get_account_balance('1100'),\n        }\n        return summary\n    \n    def generate_member_statement(self, member_id):\n        \"\"\"Generate statement showing member account activity\"\"\"\n        report = f\"\\n{'='*60}\\n\"\n        report += f\"MEMBER ACCOUNT STATEMENT - Member ID: {member_id}\\n\"\n        report += f\"{'='*60}\\n\\n\"\n        \n        transactions = self.get_member_transaction_history(member_id)\n        \n        report += f\"{'Date':<15} {'Type':<20} {'Amount':<15} {'Description':<20}\\n\"\n        report += \"-\" * 70 + \"\\n\"\n        \n        for txn in transactions:\n            report += f\"{txn[6]:<15} {txn[3]:<20} ${txn[2]:>12,.2f}  {txn[4]:<20}\\n\"\n        \n        return report\n    \n    def _get_account_id(self, account_identifier):\n        \"\"\"Get account ID from name or code\"\"\"\n        # Try as code first\n        query = \"SELECT id FROM accounts WHERE account_code = ?\"\n        result = self.db.cursor.execute(query, (account_identifier,)).fetchone()\n        if result:\n            return result[0]\n        \n        # Try as name\n        query = \"SELECT id FROM accounts WHERE name = ?\"\n        result = self.db.cursor.execute(query, (account_identifier,)).fetchone()\n        return result[0] if result else None\n    \n    def _sum_account_range(self, start_code, end_code):\n        \"\"\"Sum balances in a range of account codes\"\"\"\n        query = \"\"\"\n            SELECT SUM(balance) FROM accounts \n            WHERE account_code >= ? AND account_code <= ?\n        \"\"\"\n        result = self.db.cursor.execute(query, (start_code, end_code)).fetchone()\n        return result[0] if result[0] else 0\n    \n    def generate_trial_balance(self):\n        \"\"\"Generate trial balance showing all accounts\"\"\"\n        query = \"SELECT account_code, name, balance FROM accounts ORDER BY account_code\"\n        accounts = self.db.cursor.execute(query).fetchall()\n        \n        report = \"\\n\" + \"=\" * 70 + \"\\n\"\n        report += \"TRIAL BALANCE - COOPERATIVE CHART OF ACCOUNTS\\n\"\n        report += f\"As of: {datetime.now().strftime('%Y-%m-%d %H:%M:%S')}\\n\"\n        report += \"=\" * 70 + \"\\n\\n\"\n        report += f\"{'Code':<10} {'Account Name':<40} {'Balance':>15}\\n\"\n        report += \"-\" * 70 + \"\\n\"\n        \n        total_balance = 0\n        for code, name, balance in accounts:\n            if balance != 0:\n                report += f\"{code:<10} {name:<40} ${balance:>13,.2f}\\n\"\n                total_balance += balance\n        \n        report += \"-\" * 70 + \"\\n\"\n        report += f\"{'TOTAL':<50} ${total_balance:>13,.2f}\\n\"\n        report += \"=\" * 70 + \"\\n\"\n        \n        return report\n